services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
      - SESSION_SECRET=${SESSION_SECRET}
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
